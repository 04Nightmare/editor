## Installation: (Bash required)
Open bash terminal,
```bash
sudo apt-get install gcc make
```
Compile the program
```bash
mkdir editor
cd editor
git clone (https://github.com/04Nightmare/editor.git)
make
```
Add binary to path
```bash
sudo cp t_editor /usr/local/bin
t_editor
```
